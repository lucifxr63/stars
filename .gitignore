node_modules/
dist/
.vercel
.claude/
*.env.local
*.env.*.local
validateai-knowledge-vault/
docs/*.exe
validateai/pnpm-lock.yaml
*.log
playwright-report/
test-results/

# Material de diseño/referencia (binarios pesados, no parte del producto)
Design/
design-preview.html
cashflow/modelos.md

# Temporales del CLI de Supabase
validateai/supabase/.temp/
.env*

# Estado local del CLI de Supabase (project ref, pooler url, versiones).
# Estaba trackeado en un repo PUBLICO: no es codigo ni configuracion compartible.
supabase/.temp/

# Credenciales sueltas — nunca al repo
pass.md
